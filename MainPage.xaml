<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NetMauiDistribuidora.MainPage"
             xmlns:vm="clr-namespace:NetMauiDistribuidora.ViewModels"
             x:DataType="vm:TestViewModel"
             >
    
    <!--<ContentPage.BindingContext>
        <vm:TestViewModel/>
    </ContentPage.BindingContext>-->
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">
            <Label Text="{Binding Text}"/>
            <Button Text="Cambiar texto" Command="{Binding CambiaTextoCommand}"> </Button>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
